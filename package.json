{
  "name": "blade-walker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "test": "node scripts/verify.mjs && node --check public/sw.js && tsc --noEmit",
    "check": "npm run test && npm run build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.4.2",
    "@types/three": "^0.169.0",
    "sharp": "^0.35.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
